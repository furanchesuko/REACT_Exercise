export function AlertClock(onClickEvent) {
    /*     function handleDateTime() {
            alert(`The current date is ${new Date().toLocaleTimeString()}`)
        } */
    return <button onClick={onClickEvent.onClick}>Click Me!</button>
}